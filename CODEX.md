# Codex Bootstrap

Primary project instructions live in `CLAUDE.md`.

When starting work:
1. Read `CLAUDE.md` first.
2. Follow its project rules, architecture notes, and workflows.
3. Then read active docs in `Design/` (if provided in the task).
4. If `CODEX.md` and `CLAUDE.md` conflict, prefer `CLAUDE.md`.
5. Any requested instruction updates should be made in `CLAUDE.md` by default, unless explicitly told to update `CODEX.md` (or another file) instead.
