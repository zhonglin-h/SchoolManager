Implement the following feature: $ARGUMENTS

When done implementing:
1. Run the backend tests with `cd backend && ./gradlew test` and the frontend lint with `cd frontend && pnpm lint` (if frontend files were changed).
2. If any tests fail due to the new implementation (not pre-existing failures), update them to match the new behavior.
3. If the feature has no test coverage, add tests for the core logic.
4. Re-run tests to confirm everything passes before finishing.
